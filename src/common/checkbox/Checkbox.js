import React from 'react';
import { View, Text } from 'react-native';

export default Checkbox = ({

}) => {
  return (
    <TouchableOpacity style={ checkbox }>
      { image && <Image src={ fillIcon } /> }
    </TouchableOpacity>
  )
}